import PageNA from "./page-not-available";

let SettingsContainer = (props) => {

    return <section id="settings-container" style={{display: "none"}}>
        <PageNA />
    </section>
}

export default SettingsContainer;